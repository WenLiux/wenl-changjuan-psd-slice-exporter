# PSD / PSB 高保真切片工具 v0.3 项目简报

> 报告日期：2026-08-02  
> 当前状态：源码测试阶段，暂未制作正式发布包

## 一、项目目标

解决 Photoshop 使用传统切片导出时，超长 PSD / PSB 文件被限制宽度、无法按 1440px 等原始宽度高保真导出的问题。

当前工具可以读取 PSD / PSB 文件中的切片信息，按照原始宽度或指定宽度批量导出，并生成验证报告。

## 二、当前版本

当前开发版本为 **v0.3.0**。

v0.3 的主要变化是将原 CustomTkinter 界面迁移为网页技术渲染的 Windows 桌面客户端：

```text
React + TypeScript + CSS
          ↕
pywebview / WebView2
          ↕
Python 切片与导出服务
          ↕
PSD / PSB / Photoshop
```

软件仍然是本地 Windows 客户端，不需要浏览器标签页，也不会把文件上传到网络。

## 三、已完成功能

- PSD / PSB 文件选择与拖入
- 文档尺寸、色彩模式、位深和透明度读取
- Photoshop 切片识别与切片列表显示
- 原始宽度导出
- 指定宽度导出
- 可选择是否允许放大
- PNG / JPEG 输出
- JPEG 质量和背景颜色设置
- 色彩策略和文件命名规则
- 部分切片选择、全选和全不选
- 后台加载与导出，避免大 PSB 阻塞界面
- 实时导出进度与安全取消
- 验证报告和输出目录快捷打开
- 可选 Photoshop 高保真回退
- 设置自动保存
- 旧版 CustomTkinter 界面暂时保留作为回退

## 四、UI 优化进度

当前 Web UI 已完成：

- 克制的深色玻璃质感
- 主卡片鼠标跟随柔光
- 卡片悬浮边框与阴影反馈
- “原始宽度 / 指定宽度”滑动切换动画
- “PNG / JPEG”滑动切换动画
- 输入框、下拉框和开关的 hover、focus、pressed 状态
- JPEG 条件控件的启用与淡入过渡
- 1240 × 820 和 1100 × 720 窗口适配
- 支持系统“减少动画”设置

宽度显示逻辑已经优化：

- 文档原宽度为 1440px 时，选择“原始宽度”会显示 1440px
- 输入自定义宽度 2160px 后，切回“原始宽度”仍显示 1440px
- 再次选择“指定宽度”会恢复之前输入的 2160px

## 五、验证结果

- 前端 TypeScript 生产构建通过
- 常规自动化测试：133 项通过
- 宽度状态和 Web Bridge 相关测试：27 项通过
- 真实 `详情切片.psb` 已完成加载、预览和导出测试
- 真实文件共导出 14 张切片
- 750px 指定宽度导出验证通过
- 验证报告通过
- 导出前后源文件指纹保持不变

## 六、测试版启动

关闭已打开的旧测试窗口，然后双击项目根目录中的：

```text
启动-v0.3-测试版.cmd
```

当前测试版直接使用项目源码和已经构建的 Web UI，不属于正式发布包。

## 七、当前项目位置

```text
D:\CodexProjects\psd_slice_exporter
```

主要目录：

```text
app/          Python 核心、服务、Bridge 和桌面窗口
frontend/     React / TypeScript Web UI
tests/        自动化测试
docs/         阶段报告和迁移文档
scripts/      启动、导出和构建脚本
packaging/    正式打包配置（当前暂不执行）
```

## 八、下一阶段

当前阶段由用户继续测试 UI 和完整操作流程。

测试确认无误后再执行：

1. 汇总测试反馈并修复问题。
2. 运行最终真实 PSD / PSB 回归测试。
3. 更新版本说明和使用说明。
4. 制作 Windows 正式发布包。
5. 验证新电脑、中文路径和离线运行环境。

## 九、当前结论

v0.3 已完成 Web UI 迁移和原有核心功能接入，目前具备源码测试条件。正式打包继续暂缓，待用户确认功能和界面体验后再进行。
